language: node_js
node_js:
  - "7"

cache:
  directories:
    - node_modules

env:
  global:
    - GH_REF: github.com/zerosrat/blog-g-test.git

branches:
  only:
  - master

before_install:
  - npm i -g hexo-cli

install:
  - npm i

script:
  - hexo g

after_script:
  - cd public
  - git init
  - git config user.name "zero_ai"
  - git config user.email "zero.yu@hotmail.com"
  - git add --all
  - git commit -m "Update docs via travis"
  - git push -f "https://zerosrat:${GH_TOKEN}@${GH_REF}" master:gh-pages
# E: Build LifeCycle